import <vector.mun>;
import env.print(int) -> void;

structure box<T>
{
    T value;

    box(T value)
    {
        this.value = value;
    }
}

structure pair<K, V>
{
    K first;
    V second;

    pair<K, V>(K first, V second)
    {
        this.first = first;
        this.second = second;
    }
}

alias char = int;

alias string = vec<char>;

void main()
{
    /*
    vec<int> a = vec<int>.from_array([1, 2, 3]);
    print(a.size);


    a.push_back(42);
    print(a.size);
    print(a.get(3));

    print(a.pop_back());
    print(a.size);
    */

    string a = string.from_array([65]);

    print(a.get(0));
}